{
  "name": "autocs-frontend",
  "version": "1.0.0",
  "description": "AutoCS 2.0 Frontend - Next.js Application",
  "type": "module",
  "scripts": {
    "dev": "cd next && yarn dev",
    "build": "cd next && yarn build",
    "start": "cd next && yarn start",
    "lint": "cd next && yarn lint",
    "setup": "cd next && yarn install"
  }
}
